{
  "type": "module",
  "devDependencies": {
    "esbuild": "^0.14.49",
    "npm-run-all": "^4.1.5"
  },

  "scripts": {
    "start": "npm-run-all --parallel watch:tsc --print-label ",
    "watch:tsc": "esbuild script/Index.ts --external:../../../../../../views/goui/* --bundle --watch --sourcemap --format=esm --target=esnext --outdir=dist"
  }
}
