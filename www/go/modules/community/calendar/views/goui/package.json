{
  "type": "module",
  "scripts": {
    "start": "npm run start:sass & npm run start:esbuild",
    "start:esbuild": "esbuild src/Index.ts --external:../../../../../../views/goui/* --bundle --watch --sourcemap --format=esm --target=esnext --outdir=dist",
    "start:sass": "npx sass --watch style:dist"
  }
}
